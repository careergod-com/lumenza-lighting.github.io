document.addEventListener('DOMContentLoaded',()=>{const root=document.querySelector('[data-product]');if(!root)return;const id=root.dataset.product;const p=LUMENZA.catalog.find(x=>x.id===id);if(!p)return;const base=location.pathname.includes('/pages/')?'../':'';const imageSrc=/^https?:\/\//.test(p.image)?p.image:base+p.image;root.innerHTML=`<div class="product-detail"><div class="detail-media"><img src="${imageSrc}" data-remote="${p.remote}" alt="${p.title}"></div><div class="detail-copy"><span class="eyebrow">${p.category}</span><h1>${p.title}</h1><p class="muted">${p.description} Designed for modern residential and hospitality spaces.</p><div class="detail-price">${p.price}</div><div class="qty"><button>-</button><span>1</span><button>+</button></div><div class="detail-actions"><button class="btn" data-add="${p.id}">Add to cart</button><button class="btn light" data-wish="${p.id}">Save</button></div><div class="specs"><div><span>Finish</span><span>Matte / brushed</span></div><div><span>Light source</span><span>LED compatible</span></div><div><span>Collection</span><span>LUMENZA Core</span></div></div></div></div>`;bindGlobal()})